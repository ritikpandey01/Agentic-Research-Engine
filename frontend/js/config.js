// Configuration for WebIntel
// Note for Demo: The user has authorized the usage of this API key purely to mock the backend responses in the frontend.
// In a real application, the frontend would NEVER securely hold an API key like this; it would be processed by the backend.

const CONFIG = {
    // We are using the specific Gemini API key the user provided for this demo:
    GEMINI_API_KEY: "AIzaSyAevkPqEX4CelSbZS1LZyJhw3ROrMxEnFw",
    
    // Model Selection
    GEMINI_MODEL: "gemini-2.5-flash"
};
